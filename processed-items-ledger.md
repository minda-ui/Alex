# Processed Items Ledger — Alex (AI Housekeeping & Operations Steward)

_One row per sweep run or Help & Lessons item Alex handles (the reprocessing guard). Status: in-progress | done | partial | blocked._

_**Numbering is continuous across parts.** This file holds **item 33 onward**. Items **1–32** (2026-09-14 → 2026-09-17) are complete and unaltered in `Archive/`, in `processed-items-ledger.md (archived 2026-09-17 2010, superseded by the AX-3 history split — holds items 1–32; the live ledger continues from item 33)`, Drive id `1ASH4zZ5L0D1lG5bcufI13yA0SvfNd_xT`. Archived files keep stable ids, so that reference will not rot._

## Why this file was split, and the rule that keeps it working

On 2026-09-17 this ledger reached **45,720 B** and could no longer be written to at all. `AX-3` records
that a Drive upload silently truncates somewhere around ~31KB — no error, just a file cut off mid-word —
and a full-file archive-then-recreate is the only edit mechanism available. Past that size the
reprocessing guard becomes un-appendable: the very file that stops work being repeated cannot record that
work was done.

The split cost nothing and lost nothing, because **a Drive move is metadata-only**. The 45,720 bytes were
never re-uploaded; the original file object was renamed and moved into `Archive/` exactly as it stood, and
this small successor was created beside it. No content passed through the truncating path.

**The standing rule, from now on:** when this file passes **~25KB**, close it the same way — move it to
`Archive/` under its dated split name, and start a fresh live `processed-items-ledger.md` that continues
the item numbering and links back to the previous part. Never let it approach the ceiling again, and never
attempt a full-file recreate of anything already past it: archive it by moving, and shrink the successor.

The same shape works for any append-only file that outgrows the ceiling. It does **not** solve `AX-3` for a
large file that cannot be split — the group `CLAUDE.md` (~58KB) is one coherent document, not a log, so it
still needs either a smaller-diff write path or a deliberate content split. That part of `AX-3` stays open
and stays a decision for Minda and Eugene.

| # | Date | Item | Type | Status | Notes |
|---|---|---|---|---|---|
| 33 | 2026-09-17 | Group house rules copied into the Fishbone Properties KB (owner-instructed) | Owner-directed | done | Minda asked for a copy of `Process-Fishbone-Systems-House-Rules.md` in the Properties KB. Dry-run presented first: source confirmed live (group Wiki, v1.3, 26,844 B); destination confirmed to hold no existing copy anywhere (root, `Wiki/`, `Wiki/Processes/`), so an add rather than an overwrite. Two caveats raised before applying — it is **additive, not a Rung-2 janitorial fix** (recorded as an explicit owner decision rather than stretched to fit Rung 2), and it creates a **second home** for a rule whose single source is the group Wiki. Minda ticked and chose `Wiki/Processes/`. Applied by **native Drive copy** rather than read-and-re-upload, specifically to stay clear of `AX-3`'s ceiling — a native copy cannot truncate. Byte-verified 26,844 B == 26,844 B, new id `12CN3Ti_hLqtVRkissUUWLKM04StyEAhx`. Per-action log written into the target KB (`Outputs/change-log-2026-09-17-group-house-rules-copied-in.md`). **This row could not be written at the time** — the ledger was already past the AX-3 ceiling; backfilled here as part of the split (item 35). See `change-log/change-log-2026-09-17-house-rules-copied-to-properties.md`. |
| 34 | 2026-09-17 | Irina's access diagnosed; conventions indexed by link; onboarding step made group standard | Owner-directed / Governance | done | Minda: Irina works in the Properties project but "doesn't have access to our Google Drive." Checked the live ACLs rather than accepting the framing — **Irina is already a writer on the Properties KB** and her Claude project reads it fine; what she cannot open is the **group KB**, which is Minda-only and holds every convention. This retro-explains item 33: that copy was a workaround for a permissions gap. Rejected a folder-level share (the group Wiki also holds `Org-Fishbone-SSAS.md` and every sister company's profile; containment elsewhere is clean and worth keeping). Recommended and applied instead: six named convention files to be shared **read-only**, indexed **by link, not copy**, in `Fishbone Properties/Wiki/Group-Conventions-Index.md` (id `1NiCynFAm_GEY2EDZaFDuVsA-o_OnZt1X`, 3,301 B). Alex cannot make the shares — CHARTER §2c forbids changing Drive/Smartsheet sharing — so they went to Minda as **`AWT-0013`** (High, due 2026-09-19) with the exact six ids and an explicit do-not-share list. Also found the **Collaboration Space is shared to the `fishboneconstruction.co.uk` domain only**, so no `@fishboneproperties.co.uk` account can reach it — raised as **`AWT-0014`** (Medium). Per Minda's instruction, made the pattern a **standing onboarding step** in the group convention article `Process-Housekeeping-and-Session-Discipline.md` (archive-then-recreate, byte-verified 8,053 B, new id `1c4luzaTgwf9Ds7ggdX9AUPRNR_7ymJ8J`). Added **Minda** to the Hub's `Assigned to` picklist so owner-blocked items are trackable at all — they previously had nowhere to live. See `change-log/change-log-2026-09-17-irina-access-diagnosed-conventions-index.md`. |
| 35 | 2026-09-17 | `AX-3` ledger blockage resolved: this ledger split into parts (items 1–32 archived intact) | Setup / Governance | done | Minda: "We need to sort this." The live ledger had reached 45,720 B — past `AX-3`'s ~31KB ceiling — so item 33's row could not be written at all, and the reprocessing guard had silently stopped guarding. Fixed structurally rather than by testing the ceiling: the whole 45,720 B file was **moved** into `Archive/` (metadata-only — nothing re-uploaded, nothing truncated, id `1ASH4zZ5L0D1lG5bcufI13yA0SvfNd_xT`, bytes untouched) and this small successor created in its place, continuing the numbering from 33 and linking back. Backfilled items 33 and 34, which the blockage had prevented. Added the standing ~25KB split rule above so it cannot recur. **`AX-3` is not closed:** a chronological log splits cleanly, but a single coherent document past the ceiling (the group `CLAUDE.md`, ~58KB) still cannot be safely rewritten — that half remains open for Minda/Eugene. Also confirmed in passing that Drive's `update_file` cannot write content at all (title and parent only), so full-file recreate really is the only edit mechanism available, and splitting really is the only general answer for append-only files. |

| 36 | 2026-09-17 | Hub Drift Watch (hourly, read-only): drift found in Tasks & Requests — two Smartsheet rows not yet mirrored to the board db | Hub Drift Watch | blocked — awaiting attended session to apply | Full field-by-field compare, all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Roster:** no drift — all six employees' Connectors/Reach/Routines/Next-run text match verbatim between Smartsheet and the `roster` collection. **Achievements:** no drift — 11 Smartsheet rows / 11 `achievements` docs, all matched by title, employee, date, detail. **Help & Lessons:** no drift — 12 Smartsheet rows / 12 `help` docs, all matched by Ref, status, and answer text (including HL-0007/HL-0008/HL-0009, whose earlier ref-collision is consistently resolved on both sides). **Tasks & Requests:** the 11 overlapping rows (AWT-0001–0004, AWT-0006–0012) match exactly on status/dueDate/doneDate/response text — but Smartsheet holds two newer rows, both created this session (2026-09-17, source "Alex session 2026-09-17 — Irina access diagnosis", the same session that produced items 34–35 above), that have never been written to the board's `tasks` collection (Smartsheet: 13 rows; board: 11 docs): **AWT-0013** (assignedTo Minda, requestedBy Alex, priority High, status Open, dueDate 2026-09-19, Health Yellow — share the six group-convention Drive files read-only with Irina) and **AWT-0014** (assignedTo Minda, requestedBy Alex, priority Medium, status Open, Health Blue — decide whether the Collaboration Space should reach Fishbone Properties accounts). This routine is read-only by design and cannot call ArtifactData writes or Artifact.publish (same approval wall as HL-0012) — logging the exact old/new values here for the next attended Alex session to apply: add `AWT-0013` and `AWT-0014` to the board `tasks` collection verbatim from Smartsheet. No other drift found this run. |

| 37 | 2026-09-18 | Hub Drift Watch (hourly, read-only): AWT-0007 Done date mismatch; AWT-0013/AWT-0014 still unmirrored | Hub Drift Watch | blocked — awaiting attended session to apply | Full field-by-field compare, all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Roster:** no drift — all six employees' Connectors/Reach/Routines/Next-run text match verbatim. **Achievements:** no drift — 11 Smartsheet rows / 11 `achievements` docs match on title, employee, date, detail, linked task. **Help & Lessons:** no drift in status/Ref — 12 Smartsheet rows / 12 `help` docs match; HL-0008's board `answer` field is a slightly shorter paraphrase of the sheet's own Context-column "FINAL ANSWER" text rather than a literal copy of the sheet's Answer column — same substance, pre-existing sheet-internal duplication, not treated as new drift. **Tasks & Requests — new drift:** `AWT-0007` (Helen, "Draft a first set of 3 company-personalised social posts...") Done date — board `tasks/AWT-0007.doneDate` = `2026-09-15`, Smartsheet Task ID AWT-0007 Done date = `2026-09-16`. This is a genuine change since item 36 (2026-09-17), which explicitly confirmed doneDate matched at that time — the Smartsheet cell has evidently been edited since. **Tasks & Requests — still open from item 36:** `AWT-0013` and `AWT-0014` remain in Smartsheet (13 rows) but still absent from the board `tasks` collection (11 docs) — unchanged since item 36, not yet applied by an attended session; not re-detailed here, see item 36 for full field values. This routine is read-only by design and cannot call ArtifactData writes or Artifact.publish (same approval wall as HL-0012) — logging the exact old/new value here for the next attended Alex session to apply: set board `tasks/AWT-0007.doneDate` to `2026-09-16` to match Smartsheet, and (carried over from item 36) add `AWT-0013` and `AWT-0014` to the board `tasks` collection verbatim from Smartsheet. No other drift found this run. |

| 38 | 2026-09-18 | Hub Drift Watch (hourly, read-only): AWT-0007 Done date mismatch (item 37) resolved; AWT-0013/AWT-0014 still unmirrored | Hub Drift Watch | blocked — awaiting attended session to apply | Full field-by-field compare, all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Roster:** no drift — all six employees' Connectors/Reach/Routines/Next-run text match verbatim between Smartsheet and the `roster` collection. **Achievements:** no drift — 11 Smartsheet rows / 11 `achievements` docs match on title, employee, date, detail, linked task. **Help & Lessons:** no drift in status/Ref — 12 Smartsheet rows / 12 `help` docs match; HL-0008's board `answer` paraphrase noted in item 37 is unchanged, still not treated as new drift. **Tasks & Requests — resolved since item 37:** the `AWT-0007` Done-date mismatch flagged last run is fixed — board `tasks/AWT-0007.doneDate` now reads `2026-09-16`, matching Smartsheet (applied by an attended session between item 37 and this run). **Tasks & Requests — still open from item 36:** `AWT-0013` and `AWT-0014` remain in Smartsheet (13 rows) but are still absent from the board `tasks` collection (11 docs) — unchanged since item 36, not yet applied by an attended session; full field values already logged in item 36, not repeated here. This routine is read-only by design and cannot call ArtifactData writes or Artifact.publish (same approval wall as HL-0012) — no new value to log for this gap since item 36 already has it; the outstanding action for the next attended Alex session remains: add `AWT-0013` and `AWT-0014` to the board `tasks` collection verbatim from Smartsheet. No other drift found this run. |

| 39 | 2026-09-18 | Hub Drift Watch (hourly, read-only): AWT-0013/AWT-0014 still unmirrored (unchanged since item 36); no other drift found | Hub Drift Watch | blocked — awaiting attended session to apply | Full field-by-field compare, all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Roster:** no drift — all six employees' Connectors/Reach/Routines/Next-run text match verbatim between Smartsheet and the `roster` collection. **Achievements:** no drift — 11 Smartsheet rows / 11 `achievements` docs match on title, employee, date, detail, linked task. **Help & Lessons:** no drift — 12 Smartsheet rows / 12 `help` docs match on Ref, status, and answer text (HL-0008's board `answer` paraphrase noted in item 37 is unchanged, still not treated as new drift). **Tasks & Requests:** no change since item 38 — `AWT-0013` and `AWT-0014` remain in Smartsheet (13 rows) but are still absent from the board `tasks` collection (11 docs); AWT-0007's Done-date mismatch (item 37) stays resolved. This routine is read-only by design and cannot call ArtifactData writes or Artifact.publish (same approval wall as HL-0012) — no new value to log since item 36 already has the full field values; the outstanding action for the next attended Alex session remains: add `AWT-0013` and `AWT-0014` to the board `tasks` collection verbatim from Smartsheet. No other drift found this run. |

| 40 | 2026-09-18 | Hub Drift Watch (hourly, read-only): AWT-0013/AWT-0014 still unmirrored (unchanged since item 36); no other drift found | Hub Drift Watch | blocked — awaiting attended session to apply | Full field-by-field compare, all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Roster:** no drift — all six employees' Connectors/Reach/Routines/Next-run text match verbatim between Smartsheet and the `roster` collection. **Achievements:** no drift — 11 Smartsheet rows / 11 `achievements` docs match on title, employee, date, detail, linked task. **Help & Lessons:** no drift — 12 Smartsheet rows / 12 `help` docs match on Ref, status, and answer text (HL-0008's board `answer` paraphrase noted in item 37 is unchanged, still not treated as new drift). **Tasks & Requests:** no change since item 38 — `AWT-0013` and `AWT-0014` remain in Smartsheet (13 rows) but are still absent from the board `tasks` collection (11 docs); AWT-0007's Done-date mismatch (item 37) stays resolved. This routine is read-only by design and cannot call ArtifactData writes or Artifact.publish (same approval wall as HL-0012) — no new value to log since item 36 already has the full field values; the outstanding action for the next attended Alex session remains: add `AWT-0013` and `AWT-0014` to the board `tasks` collection verbatim from Smartsheet. No other drift found this run. |

| 41 | 2026-09-18 | Group Asset & Label Register built (owner-directed): new Smartsheet index in Fishbone Group - Documents workspace | Owner-directed | done | Minda asked where the group asset/label register should live (following on from Darius's own handoff file, `Outputs/2026-09-17-handoff-workshop-assets-for-group-register.md`, which named Alex to build it). Recommended a new Smartsheet sheet — an **index only**, per "one fact, one home" — in the **Fishbone Group - Documents** workspace, alongside the shared Document Register, maintained by Alex; detailed specs/history/maintenance stay in each KB's own Machinery Register. Minda approved ("yes, please"). Created **Group Asset & Label Register** (sheet id `5249395039799172`, `https://app.smartsheet.eu/sheets/VQ5fqFQHV792Q4QxQqjrJpCx8WWPcXhW948Rfrv1`) in workspace `5815486484113283`, 11 columns (Asset Code, Asset Label No., Description, Manufacturer, Model, Serial No., Location, Status [picklist: In service/Under repair/On order/Disposed/Sold], Owning Entity, Source of Record, Notes). Seeded 8 rows from a **live re-read** of Darius's Workshop Machinery Register (sheet `1754351980906372`), not the day-old handoff file — the re-read caught **FA2306** (ABAC GENESIS 15 500L compressor, registered 2026-09-18 via proforma invoice 208027, not in the handoff's snapshot). Two gaps carried over from the handoff, not yet resolved: FA2401 has no physical label applied; the group-wide label series 0001–0016 (used elsewhere in the estate) is not this KB's to own and hasn't been located. Reported back to Minda in chat. Did not touch Darius's own KB/Drive/Smartsheet content — new sheet only, read-only source access. |
| 42 | 2026-09-18 | Hub Drift Watch catch-up applied (attended): AWT-0013/AWT-0014 mirrored to board db + dashboard SEED, republished | Reconcile | done | Items 36–40 above (Hub Drift Watch, read-only, hourly) had flagged since 2026-09-17 that Smartsheet's `AWT-0013` and `AWT-0014` (both Open, raised by Alex for Minda re: Irina's Drive access) were never written to the board Artifact's `tasks` collection — the routine is deliberately read-only and defers writes to the next attended session. This session is attended, so applied it: re-read both rows live in Smartsheet (Tasks & Requests, sheet `8860839228606340`) to confirm no drift since item 36's logged values, confirmed still absent from the board db (11 docs), then wrote both via one atomic `ArtifactData` batch (board db now 13 task docs). Also added both to the page's SEED literal (they'd never been seeded, unlike board-db-only drift on existing rows) and republished (version 26, `SEED_SYNCED_AT` refreshed). No other drift found. Not a fresh Help & Lessons row — same open item items 36–40 already carry, now closed by an attended write rather than a new finding. |

_(Forty-two items processed to date — items 1–32 in the archived Part 1, items 33 onward here.)_
