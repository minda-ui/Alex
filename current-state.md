# Current State — Alex (AI Housekeeping & Operations Steward)

_Standing control file. Overwritten by archive-then-recreate at the end of any session that changes it — present snapshot, not history. History lives in `change-log/`. Companions: `open-issues.md` (`AX-<n>`), `external-source-register.md` (`AXSRC-<n>`), `processed-items-ledger.md`._

| Field | Value |
|---|---|
| Created | 2026-09-14 (owner-authorised, Minda), by Claude on behalf of minda@fishboneconstruction.co.uk. |
| Role | Housekeeping & Operations Steward: documentation discipline, estate tidiness, and the group **Help & Lessons** desk. Employee #9 of the AI workforce. |
| Authorised reach | **Rung 0 + Rung 1** of the control-release ladder (CHARTER §9): read the whole estate; fix **this KB and the group KB** unattended (mechanical, reversible, archive-never-trash); propose everything else. Rungs 2–4 **not** released. **Plus §9a (new, 2026-09-15, permanent):** write any row on the AI Workforce Hub for any employee — a separate axis from the KB ladder; still never another employee's own KB. |
| KB home | Drive `1QGc0EqThFDAIP7QYvGY1DEhliSbMTGNe`. Folders: `Sweeps/`, `Templates/`, `Help-Desk/`, `_escalations/`, `change-log/`, `Archive/`. |
| Git mirror | **`minda-ui/Alex`** — seeded 2026-09-14, caught up to Drive 2026-09-15 (a same-day-onward drift closed this session: AX-2's resolution, AX-3, AX-4, the Sweep digest and two change-log files had never been mirrored — all copied byte-for-byte from Drive). `AX-1` resolved. Drive stays the source of truth; the mirror is kept in step. |
| Connectors | Google Drive + Smartsheet + Web (read). No Gmail; Alex sends nothing. |
| Hub | Roster row on "Fishbone AI Workforce" (`4946803578693507`), Status **Active**; owns the **Help & Lessons** sheet (`7780569054316420`). Since §9a (2026-09-15): writes **any** row on the Hub, for any employee — not only its own. |
| Last session | **2026-09-15 — charter amended (§9a), git-mirror drift closed, Hub board synced for all employees.** Minda asked for a whole-Hub update, not only Alex's rows; confirmed as a **permanent** widening rather than one-off, recorded as new **§9a — Hub-wide write authority** (old `CHARTER.md` archived, new one created and byte-verified, 15,717 B). Found and closed a same-day drift in this git mirror (AX-2's resolution, AX-3, AX-4, the AX-2 Sweep digest and two change-log files existed in Drive but were never mirrored — all now copied byte-for-byte). Used the new §9a authority for the first time to sync the Hub board against each employee's own Drive `current-state.md`: **Peter** — Roster routines/last-next-run corrected (3 routines are live, not "pending"); `AWT-0001` → In Progress (routine live, but no discrete "combined run summary" file found — flagged, not guessed); `AWT-0002` → Done (Companies House beat-2b: 28/28 API calls OK, 0 changes). **Helen** — Roster Home KB corrected (git mirror is seeded, not "pending"); `AWT-0007` → In Progress (Amfa's 3-post set done; Construction/Properties still blocked on their own Brand-and-Voice work not existing yet). **Eugene** — checked, nothing to sync (his tasks postdate his last session). **Then found a second, separate staleness:** the AI Workforce Hub board (the published Artifact, `AXSRC-4`) keeps its **own** database, seeded once on 2026-09-14 and never touched since — the daily reconcile routine meant to keep it in step with Smartsheet (`AWT-0004`, Eugene's) was never built, so Smartsheet writes never reached the board Minda actually looks at (it still showed Alex "Building", AWT-0008/0009 "Open", no HL-0005). Synced the board's `roster`/`tasks`/`achievements`/`help` collections to match the now-current Smartsheet state (11 writes, one atomic batch) — same facts as above, mirrored into the board's own store, nothing new invented. |
| Open issues | `AX-3` (Drive upload-size tooling limit, ~31KB — blocks editing any control file already at/above that size) and `AX-4` (cross-KB undocumented-`CLAUDE.md`-edit pattern, needs a group `open-issues.md` row once AX-3 is unblocked or a human/Eugene adds it directly). `AX-1` and `AX-2` resolved. See `open-issues.md`. |
| Next action | (1) Minda/Eugene decide how to unblock AX-3 — needed before the group `CLAUDE.md` wiring and the AX-4 group-OI row can be applied. (2) A second interactive or routine sweep to work the 9 sister-KB proposed-fix lists in the digest (all read+propose only — Rung 2 not released). (3) Peter/Minda to confirm whether AWT-0001 still needs a discrete "combined run summary" document, or whether the ledger/change-log already satisfy it. (4) **`AWT-0004` (Eugene's daily reconcile-routine runbook) is the durable fix for the board-db staleness found this session** — until it exists, the board and Smartsheet will drift apart again; Alex's manual sync today is a one-off catch-up, not a substitute. Nudged: raised `AWT-0004` to **Critical** (Smartsheet + board db) with the concrete incident as evidence, at Minda's request — visible for whenever Eugene's next session picks it up (Eugene is interactive-only, no routine). (5) Continue owning the Help & Lessons desk and the whole Hub board (§9a) as new rows/drift arrive. |
