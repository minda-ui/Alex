# Current State — Alex (AI Housekeeping & Operations Steward)

_Standing control file. Overwritten by archive-then-recreate at the end of any session that changes it — present snapshot, not history. History lives in `change-log/`. Companions: `open-issues.md` (`AX-<n>`), `external-source-register.md` (`AXSRC-<n>`), `processed-items-ledger.md`._

| Field | Value |
|---|---|
| Created | 2026-09-14 (owner-authorised, Minda), by Claude on behalf of minda@fishboneconstruction.co.uk. |
| Role | Housekeeping & Operations Steward: documentation discipline, estate tidiness, and the group **Help & Lessons** desk. Employee #9 of the AI workforce. |
| Authorised reach | **Rung 0 + Rung 1 + Rung 2** of the control-release ladder (CHARTER §9): read the whole estate; fix **this KB and the group KB** unattended (mechanical, reversible, archive-never-trash); **from 2026-09-15, apply the same mechanical, reversible fixes in any sister KB under Rung 2's dry-run-then-tick guardrail — attended only** (Minda sees and approves the intended list before anything touches a sister KB; the unattended sweep routine stays propose-only); propose everything beyond that. **Rungs 3–4 not released** (normalising content shape / changing substance). **Plus §9a (2026-09-15, permanent):** write any row on the AI Workforce Hub for any employee — a separate axis from the KB ladder; still never another employee's own KB. |
| KB home | Drive `1QGc0EqThFDAIP7QYvGY1DEhliSbMTGNe`. Folders: `Sweeps/`, `Templates/`, `Help-Desk/`, `_escalations/`, `change-log/`, `Archive/`. |
| Git mirror | **`minda-ui/Alex`** — seeded 2026-09-14, kept in step with Drive. **Charter v3 pushed 2026-09-15 (commit `18ab599`):** reconciled a concurrent-write collision — a parallel session added §9a to the charter while this session authored the Rung 2 release, so the v2 push (`99652ed`) carried Rung 2 but had dropped §9a; v3 is the single merged charter (Rung 2 + §9a), byte-matching the live Drive `CHARTER.md` (19961 B). Earlier same-day drift also closed (AX-2's resolution, AX-3, AX-4, the Sweep digest and two change-log files, all copied byte-for-byte from Drive). `AX-1` resolved. Drive stays the source of truth; the mirror is kept in step. Routine-created work (the reconcile routine below) runs on its own throwaway branches per fire — not yet consolidated into this mirror's history; flagged for later tidying, not urgent. |
| Connectors | Google Drive + Smartsheet + Web (read). No Gmail; Alex sends nothing. |
| Hub | Roster row on "Fishbone AI Workforce" (`4946803578693507`), Status **Active**; owns the **Help & Lessons** sheet (`7780569054316420`). Since §9a (2026-09-15): writes **any** row on the Hub, for any employee — not only its own. **Second routine live:** `Alex — Daily Hub Reconcile` (`trig_01EMsc8Bn7c3a75q9cfr981c`, `0 5 * * *` UTC), created 2026-09-15 and first-run tested the same day — found and fixed genuine drift (trimmed board-db copies vs. fuller Smartsheet text) in its first run, confirming it works as intended. |
| Last session | **2026-09-15 — Rung 2 released, then reconciled a concurrent charter collision.** Owner decision (Minda): *"we need to give Alex Rung 2 level."* Rung 2 (mechanical, reversible janitorial writes into sister KBs) released under **mandatory dry-run-then-tick, attended only** — Minda approves the intended fix list before any sister KB is touched; the unattended sweep routine stays propose-only. Charter taken to **v3** (Rung 2 + §9a merged) via archive-then-recreate — one live `CHARTER.md` (id `1CyMXpJ_id-EDu2eli2jOPfLmAoSHUNTf`, 19961 B, byte-verified) — and pushed to git (`18ab599`). **Concurrency collision caught and reconciled (now `AX-5`, resolved):** a parallel session had added §9a to the charter at the same time this session authored the Rung-2 charter off a slightly older copy, briefly producing two `CHARTER.md` variants (one with §9a, one with Rung 2); caught by re-reading the live control files before finishing, merged into the single v3, both predecessors archived. **Earlier the same day:** §9a (Hub-wide write authority) recorded permanently; git-mirror drift closed; Smartsheet + board db synced for all employees; the dashboard board-db staleness root-caused (its `db` runtime capability was never declared at publish — fixed by republishing with `capabilities:{db:{}}`); a full Hub walkthrough with Minda (HL-0001 → Answered + `AWT-0010` for Peter; `AWT-0001` closed; `AWT-0004` reassigned to Alex and fulfilled by creating the daily reconcile routine; Helen's `AWT-0006`/`AWT-0007` root-caused to no Construction Brand-and-Voice material → `AWT-0011`). |
| Open issues | `AX-3` (Drive upload-size tooling limit, ~31KB) and `AX-4` (cross-KB undocumented-`CLAUDE.md`-edit pattern) — both still open, both a human/Eugene decision. `AX-5` (concurrent-session control-file collision) **resolved 2026-09-15** with a standing lesson: re-read the live control file's current id/size immediately before an archive-then-recreate write. `AX-1` and `AX-2` resolved. See `open-issues.md`. |
| Next action | (1) Minda/Eugene decide how to unblock AX-3/AX-4. (2) **Rung 2 is now released**, so the next sweep can, in an **attended** session, apply the 9 sister-KB proposed-fix lists as mechanical/reversible fixes under dry-run-then-tick (Minda approves the list first); the unattended routine still only proposes. (3) `AWT-0003` (Eugene, PDF-toolkit verify, due 2026-09-30) — no rush. (4) `AWT-0010` (Peter, file the FlexiLoan email, due 2026-09-16) and `AWT-0011` (Helen, Construction web research, due 2026-09-22) — both queued, need the respective employee's next session. (5) Git-mirror branch tidying (routine-created branches vs. this session's working branch) — deferred, Minda's call on timing. (6) Watch the daily reconcile routine's next few runs to confirm it stays clean now that the board db is fully caught up. |
