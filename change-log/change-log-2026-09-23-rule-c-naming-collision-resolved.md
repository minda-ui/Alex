# Change log — 2026-09-23 (third entry) — Rule-C naming collision resolved

_Append-only dated session file. See `current-state.md` and `CHARTER.md` §7._

## Session — 2026-09-23 (continued)

Minda ruled on the naming collision flagged after the group `CLAUDE.md` split (see
`change-log-2026-09-23-group-kb-claude-md-split-into-core-rules-history.md`): "Rule C is rule C; we
need to rename other rule." Plain-brief keeps the letter; the pre-existing verify-before-reporting
rule is renamed.

**Applied, Rung 1, direct edit:**

- Alex's own `Charter-Rules.md` (7,587 B → 7,583 B) — the former "Rule C" (verify against the
 system of record) relabelled **Rule E**; reordered after Rule D (board-drift) for ascending
 readability; the plain-brief cross-reference note simplified now the letter is settled rather than
 contested. Archived old, byte-verified new.
- `Charter-History.md` (3,862 B → 4,909 B) — new dated entry recording the rename and reasoning.
 Archived old, byte-verified new.
- The group's own `Process-Housekeeping-and-Session-Discipline.md` (19,334 B → 20,982 B, in the
 group KB's Wiki, also Alex's Rung-1 authority) — folded the plain-brief standard into the Hub
 Coordination Standard's own bullet list as Rule C (previously kept separate specifically to dodge
 this collision); renamed its own verify-rule to Rule E to match; added a Sources entry and a dated
 History entry. Archived old, byte-verified new.

D is deliberately left alone (Alex's own board-drift rule) so no letter is reused anywhere across
the estate's governance documents. Updated the group KB's own `current-state.md` a second time this
session (17,436 B → 17,784 B, byte-verified) and wrote its own dated change-log entry.

**Not propagated:** the seven sister `CHARTER.md` files never received Rules C/D/E in the first
place (only A/B went out in the 2026-09-20 broadcast, a day before Rule C existed) — no fix needed
there for this specific collision, though they'll want D/E eventually via the Raw/-only channel,
folded into the existing sister-charter-split proposal.

Updated this KB's own `current-state.md` (6,499 B → 6,688 B, byte-verified): Control-files row byte
counts refreshed, git-mirror gap now seven files, Last-session cell covers all three pieces of work
this session, Next-action reworked to drop the now-resolved item and fold the D/E propagation into
the existing sister-charter item.

**Pending / carried forward:** unchanged from the prior entry — sister-charter split + D/E
propagation (Rung-2, Minda to tick), `AWT-0058` recheck, `AX-14`, git-mirror catch-up (now 7 files),
the six sister KBs still owing the AWT-0034 adoption.
