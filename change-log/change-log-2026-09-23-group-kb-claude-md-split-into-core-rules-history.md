# Change log — 2026-09-23 (second entry) — group KB CLAUDE.md split into core/rules/history

_Append-only dated session file. See `current-state.md` and `CHARTER.md` §7._

## Session — 2026-09-23 (continued)

Following the same session's `CHARTER.md` split (see the earlier dated entry
`change-log-2026-09-23-charter-split-core-rules-history-real-test.md`), Minda said "yes, go ahead
with group KB" — extend the identical core/rules/history split to the group's own `CLAUDE.md`.

**Concurrent-edit check first.** Before touching anything, re-fetched the live group `CLAUDE.md`
metadata (the AX-5 discipline) rather than trusting yesterday's own edit. It had grown from 65,330 B
to 68,010 B, created minutes earlier by another session — the content showed it was Victoria's,
adding Anna's stand-up entry and the v1.4 document-policy note. Per Rule C, read the full live
file rather than assuming memory was current, and built the split from that verified state.

**Two findings surfaced while reading the full file, handled per charter §2c:**
- A small, mechanical, unambiguous inconsistency (a stale Finance-archive phrase in §6a's "must
 never do" list, left behind by the other session's own otherwise-complete v1.3→v1.4 fix) — fixed
 directly as part of the split.
- A genuine naming collision (the group file's new "Rule C — plain-brief" versus the pre-existing,
 unrelated Rule C already used across the estate's own charters, including this one) — not fixed
 unilaterally. Raised as an explicit open question in both the group KB's `current-state.md` and
 this KB's own, for Minda to decide.

**Split applied, byte-verified throughout:**
- Live `CLAUDE.md` (68,010 B) archived intact to the group's own `Archive/`.
- New `CLAUDE.md` (42,189 B) — §1–§5 and §7 (database structure, Wiki guidelines, workflow, change
 log, automated processes, group snapshot); §0/§6 replaced with pointers to `CLAUDE-Rules.md`; the
 intro's revision history replaced with a pointer to `CLAUDE-History.md`.
- `CLAUDE-Rules.md` (11,562 B, new) — §0 and §6 in full, including the Finance-archive wording fix.
 States it carries the same governance as `CLAUDE.md` itself.
- `CLAUDE-History.md` (12,854 B, new) — the full 25-entry revision history reformatted as a clean
 append-only dated list, plus a new entry for the split itself.

Content-checked against the archived 68,010 B file: every sentence preserved across the three, bar
the one deliberate wording fix. Updated the group KB's own `current-state.md` (17,495 B → 17,436 B,
byte-verified) and wrote its dated change-log entry
(`change-log-2026-09-23-claude-md-split-into-core-rules-history.md`, in the group KB's own
`change-log/`).

**Result:** the same ~75%-class reduction already proven on Alex's own `CHARTER.md` now applies to
the group KB too — an ordinary rule change touches `CLAUDE-Rules.md` (~11.5KB) plus one appended
history line, not the whole ~68KB file.

**Not fixed by this, out of Alex's hands:** the underlying Drive patch/append gap (unchanged,
already logged on the Hub `HL-0030`/`HL-0034`/`HL-0040`).

**Pending / carried forward:** raise the Rule-C naming collision to Minda directly, as its own
question (not just parked in a Next-action row) — done in this same session as a chat message.
Sister-employee charter split remains Rung-2 territory, Minda to tick which KBs. Git mirror now
six files behind Drive (non-urgent). All other outstanding items unchanged (see `current-state.md`).
