# Change log — 2026-09-16 (afternoon session backfill + weekly Housekeeping sweep)

**Type:** Backfill (undocumented-session gap) + scheduled Housekeeping sweep. **Author:** Alex (this scheduled, unattended run). **Authorised reach this run:** Rung 0 + Rung 1 only (group KB + own KB; every other KB propose-only, per the routine prompt).

## Part 1 — backfill: the 05:54 → ~20:00 gap

Alex's own `change-log/` had no entry after `change-log-2026-09-16-dashboard-staleness-diagnosed-and-fixed.md` (05:54), but `processed-items-ledger.md` and `open-issues.md` both carried activity through ~19:42/18:41, and the Hub Smartsheet (Roster/Tasks & Requests/Help & Lessons) shows edits as late as ~19:58. This is the Definition-of-Done gap this entry backfills — a dated `change-log/` file for a session's actual Drive/Hub changes, written after the fact from the ledger and Smartsheet history rather than invented.

Summary of what happened in that window (full detail in `processed-items-ledger.md` rows 9–21, which is the source of truth for this backfill):
- Dashboard `db` capability root-caused and fixed (declared `capabilities:{db:{}}` at publish); a permanent sync-status indicator added; several rounds of stale-vs-live SEED reconciliation (AWT-0006's SEED copy in particular, caught after Minda flagged it directly).
- HL-0006 (Properties `References/` folder provenance) resolved — Minda confirmed Irina's filing was intentional.
- Two separate HL-0007/HL-0008 Ref-numbering collisions between Alex's and Eugene's concurrently-working sessions, both caught and reconciled (same lesson class as AX-5: re-read live state immediately before writing/numbering, never assume).
- HL-0009 raised and answered (Helen's Roster row was missing the Smartsheet connector, same shape as Eugene's HL-0008) — Minda confirmed the Smartsheet connector is account-wide for every employee; the earlier gaps were routine/session-scoping, not missing account provisioning.
- AWT-0003 (SessionStart hook verification across all 5 KB repos) closed Done; HL-0007 (Fishbone-Group's default branch not named `main`) raised, then resolved once Eugene renamed it.
- HL-0010: Minda granted Peter a narrow §2c exception to register + file single-company, data-filling-only documents himself; refined same day from an enumerated document-type list to a "did this already happen (data-filling) vs. does it request a future action" principle, after the FC0000012 precedent. Peter's `CLAUDE.md` amended twice (25,293 B final), both archive-then-recreate, byte-verified, by Peter's own session — not by Alex.
- HL-0011 (Critical): Helen's session mistakenly `trash_file`'d Alex's own `open-issues.md` (a genuine charter §2b breach, caught and logged by Helen herself). Minda restored it from Drive Trash directly; byte-verified back to 5,370 B, no data lost. **This backfill also fixes the resulting drift**: the restored file (id `1VPYo3Skzi8IyiUoTT0oRtczJKV1sZ7d0`) and a second, independent recreation from the git mirror (id `1s28bUhpa9y3RMcP9UqhZXi97RC3EcYCy`, also 5,370 B, byte-identical content) both ended up live in the KB root — see Part 2.
- Peter's, Eugene's and Helen's own KBs were each updated by their own sessions in this window (their own `current-state.md`/`CLAUDE.md`/change-log or ledger) — those are each KB's own housekeeping, not Alex's to redo; noted here only because it explains why this window's Drive activity is spread across five KBs, not just Alex's.

No open item from this window needed a fresh `AX-<n>` — everything above resolved same-day via Minda/Eugene/Peter/Helen's own sessions, already reflected in Smartsheet (Roster, Tasks & Requests, Help & Lessons) as of this sweep.

## Part 2 — this sweep's own actions (Rung 0 + Rung 1 only)

1. **Archived a duplicate `open-issues.md`** left in this KB's root by the HL-0011 trash/restore incident above: two byte-identical (5,370 B) live copies existed — the Minda-restored original (id `1VPYo3Skzi8IyiUoTT0oRtczJKV1sZ7d0`, `createdTime` 2026-09-15T19:01:22Z) and a second recreation from the git mirror (id `1s28bUhpa9y3RMcP9UqhZXi97RC3EcYCy`, `createdTime` 2026-09-16T18:41:50Z). Kept the newer copy as the single live `open-issues.md`; archived the older one (metadata-only `update_file` move + rename, no content re-upload) to `Archive/open-issues-superseded-2026-09-15_predecessor-of-HL-0011-restore.md`. Re-verified both files' live id/size immediately before the move (AX-5 discipline).
2. **Refreshed `current-state.md`** (archive-then-recreate, byte-verified) — it was last written 2026-09-16 05:25, before all of Part 1's activity; now reflects the estate through this sweep.
3. **This digest**: `Sweeps/2026-09-16_Housekeeping-Sweep_v2.md` (per-KB drift table + proposed-fix list; supersedes the 2026-09-14 first sweep and the 2026-09-16 Rung-2 dry-run digest as the latest baseline).
4. **Group KB (Rung 1) — deferred this run.** The group KB's `current-state.md` (2026-09-16T19:56:45Z), its newest `change-log/` entry (`change-log-2026-09-16-darius-workshop-owner-and-victoria.md`, 19:58:46Z) and the Hub Roster sheet (modified 19:57:55Z) were all touched within roughly the last quarter-hour of this run starting (confirmed by re-reading live timestamps immediately before deciding) — inside the routine's concurrency-guard window. Per the v2 sweep prompt's Step 3, group-KB writes are deferred this run rather than risking a collision with what may still be a live hands-on session; nothing in the group KB looked Red regardless (control files and change-log are in step with each other).
5. **Help & Lessons desk**: no new `Open` rows (all 11 rows Answered/Resolved). Flipped **HL-0009** Status `Answered` → **Resolved** — its Answer field already records Minda's 2026-09-16 confirmation and the Roster fix, matching the same-day pattern already applied to HL-0007/HL-0008/HL-0010; nothing further to add.
6. **Hub bookkeeping (§9a)**: Alex's Roster row `Last run` was still 2026-09-14 despite the daily reconcile routine and two sweeps since — updated to today; `Next run` (2026-09-21) already correct, left as is. Appended an Achievements row for this sweep.
7. **Sister KBs (propose-only this run)** — see the digest's proposed-fix list: a duplicate `open-issues.md` in Helen's KB root (two different-sized copies, neither archived); Eugene's `current-state.md` stale relative to the HL-0007/0008 resolution, and his `CLAUDE.md` §1 Connectors line not yet updated to include Smartsheet (Roster's copy already is). Nothing applied in either KB this run.

**Note (correction, same session):** an earlier attempt to create this exact file accidentally uploaded placeholder text instead of this content (a copy-paste slip, not a tooling limit) — caught immediately via `get_file_metadata` (23 B, not the expected size), archived rather than trashed to `Archive/change-log-2026-09-16-afternoon-backfill-and-weekly-sweep_UPLOAD-ERROR-placeholder-text-see-recreate.md`, and this file created fresh in its place. Same AX-5-class discipline: verify what actually landed before trusting it.

Byte-verification: `open-issues.md` archive move confirmed 5,370 B unchanged (metadata-only, no re-upload); `current-state.md` recreate verified `fileSize` == local byte count, 0 U+FFFD, no `£` in this file's content.
