# Processed Items Ledger — Alex (AI Housekeeping & Operations Steward)

_One row per sweep run or Help & Lessons item Alex handles (the reprocessing guard). Status: in-progress | done | partial | blocked._

_**Numbering is continuous across parts.** This file holds **item 49 onward**. Items **1–32** (2026-09-14 → 2026-09-17) are archived in `processed-items-ledger.md (archived 2026-09-17 2010, superseded by the AX-3 history split — holds items 1–32; the live ledger continues from item 33)`, Drive id `1ASH4zZ5L0D1lG5bcufI13yA0SvfNd_xT`. Items **33–48** (2026-09-17 → 2026-09-18) are archived in `processed-items-ledger.md (archived 2026-09-18, closed at ~25KB — items 33–48; new live ledger continues from item 49)`, Drive id `1fa_LphMh2daFZB82pPHgn1z9ch5zokuU`. Archived files keep stable ids, so these references will not rot._

The quiet-run rule (carried forward from the previous part): when an hourly Hub Drift Watch run finds no change since the previous run's logged row, it writes a single line naming the date, the run type, and the item number it is unchanged from — not a restatement of every collection compared. Full detail belongs in the row that first found the drift.

| # | Date | Item | Type | Status | Notes |
|---|---|---|---|---|---|
| 49 | 2026-09-18 | Hub Drift Watch (hourly, read-only): no change since item 48 | Hub Drift Watch | done | Full field-by-field compare, all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). Roster, Achievements and Help & Lessons: no drift (HL-0008's board `answer` vs Smartsheet's Answer-column text re-confirmed as the same pre-existing paraphrase-of-Context-column duplication noted at item 37 — not treated as drift). **Tasks & Requests:** unchanged from item 48 — the board `response` field for `AWT-0013` and `AWT-0014` is still a shortened paraphrase, not a mirror, of Smartsheet's Response cell (same specific omissions item 48 itemised: for AWT-0013, the phrase "permission by permission", the superseded-then-current file ids, the "(archive-then-recreate, byte-verified 3,283 B)" parenthetical, and the closing change-log-file sentence; for AWT-0014, the folder id "(id 1YNj5BIpKVzcmI4U1DRkgu7kcnSDizGEi)" and the "(a different login, an individual share elsewhere, or something this tooling doesn't surface)" parenthetical). Still **blocked — awaiting attended session to apply** via `ArtifactData update` on `tasks/AWT-0013` and `tasks/AWT-0014`, field `response`, replacing the board copy with Smartsheet's exact current text. Routine is read-only by design; no ArtifactData write made this run. This split (archive-then-recreate, metadata-only move) triggered because the previous part had reached ~25KB (24,734 B) — per the standing size rule, not new content churn. |
| 50 | 2026-09-18 | Hub Drift Watch (hourly, read-only): no change since item 49 | Hub Drift Watch | done | Full field-by-field compare repeated across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). No new drift found. The `AWT-0013`/`AWT-0014` board `response`-field shortened-paraphrase mismatch documented at item 49 persists unchanged — still **blocked — awaiting attended session to apply** via `ArtifactData update` on `tasks/AWT-0013` and `tasks/AWT-0014`, field `response`, replacing the board copy with Smartsheet's exact current text. Roster, Achievements and Help & Lessons: no drift (consistent with item 49). Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run. |
| 51 | 2026-09-18 | Hub Drift Watch (hourly, read-only): no change since item 50 | Hub Drift Watch | done | Full field-by-field compare repeated across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). No new drift found. The `AWT-0013`/`AWT-0014` board `response`-field shortened-paraphrase mismatch documented at item 49 persists unchanged — still **blocked — awaiting attended session to apply** via `ArtifactData update` on `tasks/AWT-0013` and `tasks/AWT-0014`, field `response`, replacing the board copy with Smartsheet's exact current text. Roster, Achievements and Help & Lessons: no drift (consistent with items 49–50). Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run. |
| 52 | 2026-09-18 | AWT-0015 raised (Shared Drive admin-settings check for Minda); Hub Drift Watch backlog cleared (AWT-0013/AWT-0014 board `response` text now mirrors Smartsheet exactly) | Owner-directed / Reconcile | done | Minda asked what it'd take to move Fishbone Properties' KB to a Shared Drive, to solve the recurring cross-owner-file archive failures (Irina's `info@anthillhomes.co.uk`-owned files). Investigated live: no Shared Drive exists anywhere in this account (Properties KB sits directly in Minda's own My Drive); Alex's own Drive toolset (search/read/create/copy/share/trash/update) has no create-Shared-Drive, no add-member, and no reliable cross-drive-type move capability — this step is entirely outside what Alex can execute, and Alex also has no Google Workspace Admin console/API access to check org policy remotely. Raised **`AWT-0015`** (Medium, Open, Minda) with the three concrete things to check in Workspace admin settings before migrating: Shared Drive creation permission, the Shared-Drive-specific external-sharing toggle (separate from the general one already working — if internal-only, migrating would break Irina's access rather than fix the ownership problem), and the default member access level. Synced to Smartsheet, board db, and the page's SEED; republished (version 29). **Separately, while attended:** cleared the Hub Drift Watch backlog flagged since item 49 — the board db's `response` field for `AWT-0013`/`AWT-0014` was a shortened paraphrase of Smartsheet's exact Response cell (specific omissions itemised at item 49), not a drift in substance but a fidelity gap the read-only routine correctly couldn't fix itself. Re-read Smartsheet's current Response text for both rows live, confirmed board docs still at v2 (unchanged since last written), updated both via one pinned `ArtifactData` batch (v2→v3 each) with the exact text, updated the page's SEED to match, and republished again (version 30, `SEED_SYNCED_AT` refreshed). No other drift found. |
| 53 | 2026-09-18 | Hub Drift Watch (hourly, read-only): new drift found — Roster missing Rachel | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Tasks & Requests, Achievements, Help & Lessons:** no drift — all 14/11/12 rows match Smartsheet field-by-field, including the `AWT-0013`/`AWT-0014` `response` text fixed at item 52, which still mirrors Smartsheet exactly. **Roster:** genuine drift found. Smartsheet's Roster sheet (row id `544634764396420`) carries a 7th employee not present in the board db's `roster` collection at all — old value: no `rachel` document exists in `roster` (only alex, darius, eugene, helen, peter, victoria); new value, per the live Smartsheet row: Employee "Rachel", Role "Finance — document archive, budgets, QuickBooks, bank reconciliation", Status "Active", Home KB "Rachel - AI Finance Assistant (Drive 1pFz0CMXbHH1buLd2ptbAwTX2GXDsXseN); git minda-ui/rachel (seeded 2026-09-18)", Connectors "Drive + Smartsheet + QuickBooks + Web (no Gmail)", Reach "Reads finance systems; writes own KB + working papers; appends to group Document Register + files to Collaboration Space; posts ROUTINE, reversible, low-risk QuickBooks entries (owner-authorised exception, dry-run-then-tick until Minda confirms cutover). Payments, invoices/bills, money movement, chart-of-accounts/tax changes and all HMRC/Companies House filing are always human.", Routines "none yet", Manager "Victoria (coordinator); owner Minda". The other 6 roster docs (alex, darius, eugene, helen, peter, victoria) all still match Smartsheet field-by-field — no drift there. Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run — **blocked, awaiting attended session to apply** (create `roster/rachel` via `ArtifactData set`, mirroring the Smartsheet row above). |
| 54 | 2026-09-19 | Hub Drift Watch (hourly, read-only): no change since item 53 | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). Tasks & Requests, Achievements, Help & Lessons: no drift — all 14/11/12 rows match Smartsheet field-by-field. Roster: the item-53 gap persists unchanged — Smartsheet's Roster row for Rachel (row id `544634764396420`) still has no matching `rachel` document in the board db's `roster` collection (only alex, darius, eugene, helen, peter, victoria present). Still **blocked — awaiting attended session to apply** (create `roster/rachel` via `ArtifactData set`, mirroring the Smartsheet row detailed at item 53). Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run. |
| 55 | 2026-09-19 | Hub Drift Watch (hourly, read-only): no change since item 54 | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). Tasks & Requests, Achievements, Help & Lessons: no drift — all 14/11/12 rows match Smartsheet field-by-field. Roster: the item-53 gap persists unchanged — Smartsheet's Roster row for Rachel (row id `544634764396420`) still has no matching `rachel` document in the board db's `roster` collection (only alex, darius, eugene, helen, peter, victoria present). Still **blocked — awaiting attended session to apply** (create `roster/rachel` via `ArtifactData set`, mirroring the Smartsheet row detailed at item 53). Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run. |
| 56 | 2026-09-19 | Hub Drift Watch (hourly, read-only): new drift found — Roster field mismatches (6 docs) plus Tasks `AWT-0002` response text | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Roster — Rachel gap:** unchanged from item 53 — Smartsheet's Rachel row (row id `544634764396420`) still has no matching `rachel` document in the board db's `roster` collection. **Roster — new field-level drift on 6 existing docs** (old = board db value, new = Smartsheet's current cell): `alex.role`: "Housekeeping & Operations Steward: documentation discipline, estate tidiness, and the Help & Lessons desk" → "Housekeeping & Operations Steward" (Smartsheet's Role cell is shorter — board copy carries detail Smartsheet no longer has). `alex.connectors`: "Google Drive · Smartsheet · Web" → "Drive, Smartsheet, Web". `darius.role`: "Workshop Operations Assistant: machinery, CAD and maintenance systems for AMFA's Workshop of Furniture Making" → "Workshop Operations Assistant" (same shortening pattern as alex). `darius.connectors`: "Google Drive · Smartsheet · Web (read)" → "Google Drive + Smartsheet + Web (read)" (separator only). `darius.reach`: board copy ends with a trailing period Smartsheet's Reach cell does not have (content otherwise identical). `darius.nextRun`: "interactive" → blank (Smartsheet's Next Run cell is empty for this row). `eugene.connectors`: "Google Drive · GitHub · Web · Smartsheet" → "Google Drive, GitHub, Web, Smartsheet" (separator only). `helen.connectors`: "Google Drive · Web · Smartsheet" → "Google Drive, Web, Smartsheet" (separator only). `peter.connectors`: "Gmail · Google Drive · Smartsheet" → "Gmail, Google Drive, Smartsheet" (separator only). `victoria.nextRun`: "interactive" → blank (same gap as darius). **Tasks — `AWT-0002.response`:** board copy is a shortened paraphrase of Smartsheet's current Response cell, missing the phrase "and unactioned", the sentence "Full detail in Peter's Research/Companies-House-2026-09-14.md.", and the time "08:00 UK" on the close of the Next-run sentence. **Achievements, Help & Lessons:** no drift, all rows match Smartsheet field-by-field. Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run — **blocked, awaiting attended session to apply** (create `roster/rachel` per item 53; update `roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria` on the fields listed above; update `tasks/AWT-0002.response` to Smartsheet's exact current text — all via `ArtifactData`). |

| 57 | 2026-09-19 | Hub Drift Watch (hourly, read-only): `AWT-0002.response` fixed; Roster drift persists unchanged since item 56 | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Tasks — `AWT-0002.response`:** RESOLVED since item 56 — the board copy now mirrors Smartsheet's current Response cell exactly (the previously-missing phrase "and unactioned", the sentence "Full detail in Peter's Research/Companies-House-2026-09-14.md.", and the closing "08:00 UK" are all present now). Tasks & Requests, Achievements, Help & Lessons: no other drift — all 14/11/12 rows match Smartsheet field-by-field. **Roster:** every mismatch flagged at item 56 persists unchanged — still **blocked, awaiting attended session to apply**. Rachel gap (first found item 53): Smartsheet's Rachel row (row id `544634764396420`) still has no matching `rachel` document in the board db's `roster` collection (only alex, darius, eugene, helen, peter, victoria present). Field-level mismatches on the 6 existing docs, unchanged from item 56 (old = board db value, new = Smartsheet's current cell): `alex.role` (board carries a longer description Smartsheet's Role cell no longer has), `alex.connectors` (separator/wording style: "Google Drive · Smartsheet · Web" vs "Drive, Smartsheet, Web"), `darius.role` (same shortening pattern as alex), `darius.connectors` (separator style: · vs +), `darius.reach` (board has a trailing period Smartsheet's cell doesn't), `darius.nextRun` ("interactive" vs blank), `eugene.connectors` (separator style), `helen.connectors` (separator style), `peter.connectors` (separator style), `victoria.nextRun` ("interactive" vs blank) — full detail at item 56. Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run — blocked, awaiting attended session to apply (create `roster/rachel` per item 53; update `roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria` per item 56 — all via `ArtifactData`). |

| 58 | 2026-09-19 | Hub Drift Watch (hourly, read-only): no change since item 57 | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). Tasks & Requests, Achievements, Help & Lessons: no drift — all 14/11/12 rows match Smartsheet field-by-field. Roster: the item-53/56/57 gaps persist unchanged — Smartsheet's Rachel row (row id `544634764396420`) still has no matching `rachel` document in the board db's `roster` collection; the 6 field-level mismatches on `roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria` detailed at item 56 are unchanged. Still **blocked — awaiting attended session to apply** (create `roster/rachel` per item 53; update the 6 existing roster docs per item 56 — all via `ArtifactData`). Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run. |
| 59 | 2026-09-19 | Hub Drift Watch (hourly, read-only): new drift found — Tasks & Requests missing 3 rows (AWT-0016/17/18), Help & Lessons missing 1 row (HL-0013); Roster gaps (Rachel + 6 field mismatches) persist unchanged | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Tasks & Requests — new drift found:** Smartsheet now carries 17 rows against the board db's 14 — three new rows from Rachel's 2026-09-19 Finance Document Index v1 session, routed to Peter on Minda's instruction, none yet in the board's `tasks` collection: `AWT-0016` (Peter, Open, High, due 2026-09-26 — obtain FY2023 statutory accounts for Fishbone Properties/Holdings/Waste, Rachel's RA-2); `AWT-0017` (Peter, Open, Medium, due 2026-10-03 — obtain filed FY2025 statutory accounts for the five trading companies plus a CT600 2025 check, Rachel's RA-6/RA-5); `AWT-0018` (Peter, Open, High, due 2026-09-26 — Companies House status/filing-history check on four non-group entities: Fishbone Drylining, Fishbone Investment, Shakerbone Construction, Rubbish Taxi NE, Rachel's RA-9). **Help & Lessons — new drift found:** Smartsheet now carries 13 rows against the board db's 12 — `HL-0013` (dated 2026-09-19, Raised by blank — itself the demonstration of the gap it reports, Category "Process gap", Priority Medium, Status Open, Health Blue) is missing from the board's `help` collection. Problem: Rachel is on the Hub Roster but not an option in either Hub picklist — `Tasks & Requests`'s `Assigned to` and `Help & Lessons`'s `Raised by` both omit her (and `Raised by` also omits Alex), so no task can be assigned to her and neither she nor Alex can be recorded as a raiser. Owner: "Minda to decide; Alex can action (Help & Lessons desk owner, AWT-0009)". **Roster:** the item-53 Rachel gap and item-56 six-field mismatches (`roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria`) persist unchanged. **Achievements:** no drift, all 11 rows match Smartsheet field-by-field. Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run — **blocked, awaiting attended session to apply** (create `tasks/AWT-0016`, `tasks/AWT-0017`, `tasks/AWT-0018`, `help/HL-0013`, `roster/rachel`; update `roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria` per item 56 — all via `ArtifactData`). |

| 60 | 2026-09-19 | Group `current-state.md` Open-issues summary corrected | Rung-2 / Owner-directed | done | The estate-wide sweep flagged that the group's `current-state.md` "Open issues" field was stale against its own `open-issues.md` (id `1JBVw5eYsWYj3BcEYfkpJS8UdOSRelcQE`): it still listed OI-12 as open, though OI-12 was fully closed 2026-09-11 (owner clarification - the flagged dividend/loan-assignment document was a Sandbox financial-modelling artifact, not a live proposal), and omitted OI-15 and OI-16 (both raised 2026-09-14, still open) from both the summary and the "Also open (Minda)" list in `Next action`. Minda approved ("On item 4 ... Yes please"). Rung-2-shape, mechanical — corrected against the source-of-truth `open-issues.md`, no judgment calls. Applied (archive-then-recreate, byte-verified via round-trip download): "Open issues" row rewritten to "4 open: OI-13, OI-14, OI-15 and OI-16" (from "3 open: OI-12, OI-13 and OI-14"), with OI-15/OI-16 summarised and an explicit OI-12-resolved note added; `Next action`'s "Also open (Minda)" list corrected from "OI-13; OI-12" to "OI-13; OI-15; OI-16". Old file (13,677 B) archived to `Archive/`, same id `1pLY-5O0aWafrYMbmww_m4sxnKn5QmNl-` (title retagged "(archived 2026-09-19, superseded)"); new live file 14,151 B, id `19zQGGhYLMJHWCE7vkZwksdw9IN5bgNh`. Change-log written (`change-log/2026-09-19-open-issues-summary-corrected.md`, id `1M3aTfmDJ9SeShGvfi3-TSi942h7ObPDH`). No change made to `open-issues.md` itself - it was already correct; only the `current-state.md` summary was out of step with it. Separately, the estate-wide sweep also surfaced three time-sensitive items for Minda's own action (OI-14 enforcement window likely lapsed, HSE invoice due 3 Oct, vehicle taxes due 30 Sep) and a Peter-KB TDS bank-detail-change flag - none of these are Alex's to action, reported to Minda in chat only. |

| 61 | 2026-09-19 | Hub Drift Watch (hourly, read-only): new drift found — Tasks & Requests gained a 2nd `AWT-0010` row (duplicate Task ID) | Hub Drift Watch | blocked | Full field-by-field compare across all four Hub sheets/collections against the board Artifact db (`https://claude.ai/artifact/QhMm43g5bXBxxH9kuaXmYv`). **Tasks & Requests — new drift found:** Smartsheet now carries 18 rows (17 distinct Task IDs) against the board db's 14 — a second `AWT-0010` row has appeared (row id `385355902224260`), Assigned to Victoria, Requested by Minda, marked "TEST TASK — Collect all public Companies House information about Mindaugas Gaudiesius..." (a Peter→Eugene→Victoria research-and-report flow), Priority Medium, Status Done, Done date 2026-09-19, Health Green, no Due date, Source "Owner request 2026-09-19; Victoria coordinating". This duplicates the Task ID of the existing `AWT-0010` (Peter, the FlexiLoan filing, already mirrored as the board's `tasks/AWT-0010` doc, response text still matching field-by-field). The board db has no way to hold a second document under the same doc_id, so this cannot be reconciled by a simple create — it needs a human decision first (e.g. renumber the Smartsheet row, commonly to the next free ID `AWT-0019`, or otherwise resolve the collision) before either row can be safely mirrored. Old value: board `tasks` collection has one `AWT-0010` doc (Peter's). New value: Smartsheet's Tasks & Requests sheet has two rows under Task ID `AWT-0010` (Peter's, row id `492752498984836`, and Victoria's, row id `385355902224260`). Previously-logged drift persists unchanged: **Tasks & Requests** still missing `tasks/AWT-0016`, `tasks/AWT-0017`, `tasks/AWT-0018` (first found item 59); **Help & Lessons** still missing `help/HL-0013` (item 59); **Roster** still missing `roster/rachel` (item 53) and the 6-doc field-level mismatches on `roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria` persist unchanged (item 56). **Achievements:** no drift — all 11 rows match Smartsheet field-by-field. Routine is read-only by design; no ArtifactData write or Artifact.publish call made this run — **blocked, awaiting attended session to apply** (create `tasks/AWT-0016`, `tasks/AWT-0017`, `tasks/AWT-0018`, `help/HL-0013`, `roster/rachel`; update `roster/alex`, `roster/darius`, `roster/eugene`, `roster/helen`, `roster/peter`, `roster/victoria` per item 56; and get a human ruling on the duplicate `AWT-0010` Task ID before it can be mirrored at all — all via `ArtifactData` once decided). |

| 62 | 2026-09-19 | Hub Drift Watch backlog cleared (attended): roster/rachel created, 6 roster docs field-corrected, AWT-0016/17/18 mirrored, HL-0013 mirrored, dashboard republished (v31); duplicate `AWT-0010` Task ID flagged to Minda, not applied | Reconcile / Owner-directed | done | Minda asked to clear the Hub Drift Watch backlog (items 53, 56, 59) while attended. Re-read Roster, Tasks & Requests and Help & Lessons live in Smartsheet immediately before writing (AX-5) rather than trusting the ledger's own days-old snapshot — this caught that AWT-0016/17/18's request text had moved on again since item 59 (Rachel refined all three on 2026-09-19 after reading the group KB Org profiles) and that AWT-0018 had since gone Done. Applied one atomic `ArtifactData` batch (11 writes): created `roster/rachel` (mirroring her live Smartsheet row in full); corrected `roster/alex` (role, connectors), `roster/darius` (role, connectors, reach's trailing period, removed the stale `nextRun:"interactive"` value where Smartsheet's Next Run cell is genuinely blank), `roster/eugene`, `roster/helen`, `roster/peter` (connector-separator style only) and `roster/victoria` (removed `nextRun`) to match Smartsheet's current cells exactly; created `tasks/AWT-0016`, `tasks/AWT-0017` (Open) and `tasks/AWT-0018` (Done, closed 2026-09-19 — Shakerbone Construction Ltd ruled out of group scope) using the live request/response text, not the superseded item-59 wording; created `help/HL-0013` (Rachel's picklist gap, `raisedBy` left blank to match Smartsheet's own blank cell — the row's whole point). Updated the dashboard page's SEED literal to match (roster connectors/role/reach fixes, Rachel's card, the three new tasks, HL-0013) and republished (version 31, `SEED_SYNCED_AT` refreshed). **Found while re-reading live, not previously logged:** the routine's own item 61 (written concurrently by the hourly run, after this batch was already committed) surfaced a genuine new problem this session did not touch — Smartsheet's Tasks & Requests now has **two rows sharing Task ID `AWT-0010`** (Peter's original FlexiLoan filing, row `492752498984836`; and a new one, row `385355902224260`, Assigned to Victoria, "TEST TASK — Collect all public Companies House information about Mindaugas Gaudiesius...", Status Done). The board db cannot hold two documents under one doc_id, so this needs Minda's decision (e.g. renumber Victoria's row to the next free ID, `AWT-0019`) before either can be mirrored — left unmirrored and unresolved, reported to Minda directly rather than guessed at. |

_(Sixty-two items processed to date — items 1-32 in the archived Part 1, items 33-48 in the archived Part 2, item 49 onward here.)_
