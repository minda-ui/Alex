# Open Issues — Alex (AI Housekeeping & Operations Steward)

_The `AX-<n>` table. A resolved issue gets a `Resolved` line, never a deletion. Alex's private issue log; cross-employee problems go on the group **Help & Lessons** sheet instead._

| ID | Opened | Status | Issue |
|---|---|---|---|
| AX-1 | 2026-09-14 | Resolved | **Git mirror created + seeded.** Minda created `minda-ui/Alex`; Claude seeded `main` (commit `00e0a8a`) from this Drive KB — charter + four control files + working folders + README, byte-verified against Drive. **Resolved 2026-09-14.** Drive stays the source of truth; the mirror is kept in step. |
| AX-2 | 2026-09-14 | Resolved | **First estate sweep completed.** Read-only drift scan of all 11 KBs Alex has reach into; digest written to `Sweeps/2026-09-14_Housekeeping-Sweep_v1.md` (per-KB Green/Red table + proposed fixes). Rung-1 fixes applied in the group KB (recreated a missing `current-state.md`; registered the AI Workforce Hub as SRC-40–45; wired Helen/Alex/the Hub into `Wiki/00_INDEX.md`) and in this KB (backfilled a missing change-log entry for its own creation). Help & Lessons desk triaged (HL-0001–0004; HL-0005 added). **Resolved 2026-09-14** — see AX-3 and AX-4 for what it could not finish. |
| AX-3 | 2026-09-14 | **Resolved** | **Drive upload-size tooling limit — `create_file` silently truncates around ~31KB.** Discovered mid-sweep: an intended 62,543-byte edit to the group `CLAUDE.md` uploaded as only 31,316 bytes, cut off mid-word, with no error. Caught on byte-verification (`get_file_metadata`), safely reverted (the pre-existing 57,925-byte `CLAUDE.md` was restored byte-for-byte via a metadata-only `update_file` move from its archived copy — no content re-upload, no data lost). **Consequence:** any control file already at or above roughly 30KB (the group `CLAUDE.md` ~58KB; the group `open-issues.md` ~35KB) cannot currently be safely edited by Alex in one pass — a full-file archive-then-recreate write is the only edit mechanism available, and it silently corrupts content past the ceiling. Logged as HL-0005 for the wider team. **Needs:** either a smaller-diff Drive-write tool/path, or splitting these large files' oldest historical content into a dated archive/appendix so the live file stays under the ceiling — a decision for Minda/Eugene, not something Alex should do unilaterally to someone else's control file. Until resolved, the deferred `CLAUDE.md` §1/§5 Helen+Alex+Hub wiring (see the Sweep digest) stays a proposed fix, not applied. **Partially resolved 2026-09-17 (append-only files only).** Alex's own `processed-items-ledger.md` hit 45,720 B and became un-appendable — the reprocessing guard could no longer record that work had been done, which is how this stopped being a background issue. Fixed structurally rather than by working out where the ceiling sits: a Drive **move is metadata-only**, so the whole 45,720 B file was moved into `Archive/` exactly as it stood (id `1ASH4zZ5L0D1lG5bcufI13yA0SvfNd_xT`, holding items 1–32, nothing re-uploaded and nothing truncated) and a small successor created beside it, continuing the item numbering from 33 and linking back. A standing rule now lives in the ledger's own header: **split at ~25KB**, never let it approach the ceiling, and never attempt a full-file recreate of anything already past it. Also confirmed this session that Drive's `update_file` cannot write content at all (title and parent only), so full-file recreate genuinely is the only edit mechanism and splitting genuinely is the only general answer for an append-only file. **Still open for the rest:** a single coherent document past the ceiling cannot be split this way — the group `CLAUDE.md` (~58KB) and the group `open-issues.md` (~35KB) are documents, not logs, so they still need either a smaller-diff Drive-write path or a deliberate content split, and that remains a decision for Minda/Eugene rather than something Alex does unilaterally to someone else's control file. **RESOLVED 2026-09-18 — the limit no longer exists.** Before restructuring anything, the premise was re-tested rather than trusted: a 40-line filler file was uploaded to this KB's `Archive/` and **60,185 bytes stored, then downloaded and verified byte-for-byte** — both markers intact, all 40 lines present, zero U+FFFD (test artifact kept as evidence, id `1T1aRdYBl86aHau5uIzF7eqF5quP4aB_E`). 60,185 B exceeds both stuck files (group `CLAUDE.md` 57,925 B; group `open-issues.md` 35,143 B), so **both are now safely writable and no split, appendix or content restructure is needed anywhere**. Whatever changed in the tooling between 2026-09-14 and 2026-09-18, the ~31KB truncation is gone. The 2026-09-17 ledger split remains worthwhile on its own merits (a 45,720 B log is unwieldy regardless) and its ~25KB rule stands as good practice, not as a workaround. **Lesson worth keeping:** a tooling limitation recorded three days ago is a hypothesis, not a fact — re-test before acting on it. Had this not been tested first, the estate's most important file would have been carved up to solve a problem that had already fixed itself. |
| AX-4 | 2026-09-14 | **Resolved** | **Cross-KB pattern: undocumented `CLAUDE.md` edits in six of nine read-only KBs.** This sweep found the same shape in Properties, Holdings, Waste, SSAS, Peter and Eugene: a root `CLAUDE.md`/control-file edit landed after that KB's own change-log/register was last updated, with nothing logging it. Holdings and Waste specifically trace to one shared event (the 2026-09-11 "Group house-rules" restructure) landing in both KBs' `CLAUDE.md` with no change-log entry in either. **This is group-level drift and belongs on the group KB's `open-issues.md`, which Alex is authorised to add to (Rung 1) — but the group `open-issues.md` is already ~35KB, over the same AX-3 upload ceiling, so Alex cannot safely append to it this session.** Recorded here instead, and in the Sweep digest, until either AX-3 is resolved or a human/Eugene appends the `OI-<n>` row directly. **Proposed row for the group `open-issues.md` once appendable:** "OI-<n> — Six sister KBs (Properties, Holdings, Waste, SSAS, Peter, Eugene) have undocumented `CLAUDE.md`/control-file edits with no matching change-log entry; Holdings and Waste both trace to the 2026-09-11 Group house-rules restructure. Each owning employee/session should add the missing change-log entry and refresh its register." **RESOLVED 2026-09-18 — fixed at source, so the tracking row is moot.** The proposed group `OI-<n>` row was only ever a way to ask each owning KB to document its undocumented edit. The **2026-09-16 first attended Rung-2 cycle did the documenting itself**, in all six KBs: Properties, Waste and SSAS each received a backfilled change-log entry; Holdings had five stray `CLAUDE.md` copies archived; Eugene received a backfill for his 2026-09-14 20:06 §2e edit plus a refreshed `current-state.md`; and Peter's finding had already been closed by his own session before the cycle ran. **Spot-verified on Drive 2026-09-18**, not taken on trust from the ledger: `Eugene/change-log/change-log-2026-09-16-claude-md-edit-backfill.md` (1,426 B) and `Fishbone Waste/Outputs/change-log-2026-09-16-house-rules-restructure-backfill.md` (1,313 B) — the latter documenting the exact 2026-09-11 Group house-rules restructure this issue named. Raising a group issue asking people to do what has already been done would be noise, so none was raised. Nothing was written to the group `open-issues.md`. |
| AX-5 | 2026-09-15 | Resolved | **Concurrent-session control-file collision during the Rung-2 release.** While this session authored Alex's Rung-2 charter (off a slightly older copy of `CHARTER.md`), a parallel session added **§9a (Hub-wide write authority)** to the charter. The two edits briefly produced two `CHARTER.md` variants — one carrying §9a, one carrying the Rung-2 release (pushed to git as `99652ed`, which had dropped §9a). **Caught before finishing** by re-reading the live control files (Alex's own `current-state.md` revealed §9a had already landed); the two variants were merged into a single **v3** charter (both Rung 2 and §9a), both predecessors archived-then-recreated to one live `CHARTER.md` (id `1CyMXpJ_id-EDu2eli2jOPfLmAoSHUNTf`, 19961 B), and git corrected to v3 (`18ab599`). No data lost. **Resolved 2026-09-15 with a standing lesson (now Alex's own discipline, and worth propagating estate-wide):** before any archive-then-recreate write to a control file, re-read that file's *current* live id and size immediately beforehand and author the edit onto that live copy — never onto a copy read earlier in the session — so a concurrent write cannot be silently overwritten or duplicated. Related to AX-4's undocumented-edit pattern (both are concurrency/attribution drift); logged for the team as a housekeeping convention, not a tooling blocker. |

_(**All issues resolved as at 2026-09-18.** AX-1 and AX-2 resolved 2026-09-14; AX-5 resolved 2026-09-15; AX-3 and AX-4 resolved 2026-09-18 — AX-3 by re-testing and finding the upload ceiling gone, AX-4 by the Rung-2 cycle having fixed the drift at source. No `AX-<n>` is open. Resolved issues keep their full history above; nothing is deleted.)_
