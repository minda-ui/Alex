# Open Issues — Alex (AI Housekeeping & Operations Steward)

_The `AX-<n>` table. A resolved issue gets a `Resolved` line, never a deletion. Alex's private issue log; cross-employee problems go on the group **Help & Lessons** sheet instead._

| ID | Opened | Status | Issue |
|---|---|---|---|
| AX-1 | 2026-09-14 | Resolved | **Git mirror created + seeded.** Minda created `minda-ui/Alex`; Claude seeded `main` (commit `00e0a8a`) from this Drive KB — charter + four control files + working folders + README, byte-verified against Drive. **Resolved 2026-09-14.** Drive stays the source of truth; the mirror is kept in step. |
| AX-2 | 2026-09-14 | Resolved | **First estate sweep completed.** Read-only drift scan of all 11 KBs Alex has reach into; digest written to `Sweeps/2026-09-14_Housekeeping-Sweep_v1.md` (per-KB Green/Red table + proposed fixes). Rung-1 fixes applied in the group KB (recreated a missing `current-state.md`; registered the AI Workforce Hub as SRC-40–45; wired Helen/Alex/the Hub into `Wiki/00_INDEX.md`) and in this KB (backfilled a missing change-log entry for its own creation). Help & Lessons desk triaged (HL-0001–0004; HL-0005 added). **Resolved 2026-09-14** — see AX-3 and AX-4 for what it could not finish. |
| AX-3 | 2026-09-14 | **Resolved** | **Drive upload-size tooling limit — `create_file` silently truncates around ~31KB.** Discovered mid-sweep: an intended 62,543-byte edit to the group `CLAUDE.md` uploaded as only 31,316 bytes, cut off mid-word, with no error. Caught on byte-verification (`get_file_metadata`), safely reverted (the pre-existing 57,925-byte `CLAUDE.md` was restored byte-for-byte via a metadata-only `update_file` move from its archived copy — no content re-upload, no data lost). **Consequence:** any control file already at or above roughly 30KB (the group `CLAUDE.md` ~58KB; the group `open-issues.md` ~35KB) cannot currently be safely edited by Alex in one pass — a full-file archive-then-recreate write is the only edit mechanism available, and it silently corrupts content past the ceiling. Logged as HL-0005 for the wider team. **Needs:** either a smaller-diff Drive-write tool/path, or splitting these large files' oldest historical content into a dated archive/appendix so the live file stays under the ceiling — a decision for Minda/Eugene, not something Alex should do unilaterally to someone else's control file. Until resolved, the deferred `CLAUDE.md` §1/§5 Helen+Alex+Hub wiring (see the Sweep digest) stays a proposed fix, not applied. **Partially resolved 2026-09-17 (append-only files only).** Alex's own `processed-items-ledger.md` hit 45,720 B and became un-appendable — the reprocessing guard could no longer record that work had been done, which is how this stopped being a background issue. Fixed structurally rather than by working out where the ceiling sits: a Drive **move is metadata-only**, so the whole 45,720 B file was moved into `Archive/` exactly as it stood (id `1ASH4zZ5L0D1lG5bcufI13yA0SvfNd_xT`, holding items 1–32, nothing re-uploaded and nothing truncated) and a small successor created beside it, continuing the item numbering from 33 and linking back. A standing rule now lives in the ledger's own header: **split at ~25KB**, never let it approach the ceiling, and never attempt a full-file recreate of anything already past it. Also confirmed this session that Drive's `update_file` cannot write content at all (title and parent only), so full-file recreate genuinely is the only edit mechanism and splitting genuinely is the only general answer for an append-only file. **Still open for the rest:** a single coherent document past the ceiling cannot be split this way — the group `CLAUDE.md` (~58KB) and the group `open-issues.md` (~35KB) are documents, not logs, so they still need either a smaller-diff Drive-write path or a deliberate content split, and that remains a decision for Minda/Eugene rather than something Alex does unilaterally to someone else's control file. **RESOLVED 2026-09-18 — the limit no longer exists.** Before restructuring anything, the premise was re-tested rather than trusted: a 40-line filler file was uploaded to this KB's `Archive/` and **60,185 bytes stored, then downloaded and verified byte-for-byte** — both markers intact, all 40 lines present, zero U+FFFD (test artifact kept as evidence, id `1T1aRdYBl86aHau5uIzF7eqF5quP4aB_E`). 60,185 B exceeds both stuck files (group `CLAUDE.md` 57,925 B; group `open-issues.md` 35,143 B), so **both are now safely writable and no split, appendix or content restructure is needed anywhere**. Whatever changed in the tooling between 2026-09-14 and 2026-09-18, the ~31KB truncation is gone. The 2026-09-17 ledger split remains worthwhile on its own merits (a 45,720 B log is unwieldy regardless) and its ~25KB rule stands as good practice, not as a workaround. **Lesson worth keeping:** a tooling limitation recorded three days ago is a hypothesis, not a fact — re-test before acting on it. Had this not been tested first, the estate's most important file would have been carved up to solve a problem that had already fixed itself. **Confirmed still gone 2026-09-21:** this sweep archive-then-recreated the group `open-issues.md` at 36,770 B (see AX-6/OI-17) without incident, byte-verified. |
| AX-4 | 2026-09-14 | **Resolved** | **Cross-KB pattern: undocumented `CLAUDE.md` edits in six of nine read-only KBs.** This sweep found the same shape in Properties, Holdings, Waste, SSAS, Peter and Eugene: a root `CLAUDE.md`/control-file edit landed after that KB's own change-log/register was last updated, with nothing logging it. Holdings and Waste specifically trace to one shared event (the 2026-09-11 "Group house-rules" restructure) landing in both KBs' `CLAUDE.md` with no change-log entry in either. **This is group-level drift and belongs on the group KB's `open-issues.md`, which Alex is authorised to add to (Rung 1) — but the group `open-issues.md` is already ~35KB, over the same AX-3 upload ceiling, so Alex cannot safely append to it this session.** Recorded here instead, and in the Sweep digest, until either AX-3 is resolved or a human/Eugene appends the `OI-<n>` row directly. **Proposed row for the group `open-issues.md` once appendable:** "OI-<n> — Six sister KBs (Properties, Holdings, Waste, SSAS, Peter, Eugene) have undocumented `CLAUDE.md`/control-file edits with no matching change-log entry; Holdings and Waste both trace to the 2026-09-11 Group house-rules restructure. Each owning employee/session should add the missing change-log entry and refresh its register." **RESOLVED 2026-09-18 — fixed at source, so the tracking row is moot.** The proposed group `OI-<n>` row was only ever a way to ask each owning KB to document its undocumented edit. The **2026-09-16 first attended Rung-2 cycle did the documenting itself**, in all six KBs: Properties, Waste and SSAS each received a backfilled change-log entry; Holdings had five stray `CLAUDE.md` copies archived; Eugene received a backfill for his 2026-09-14 20:06 §2e edit plus a refreshed `current-state.md`; and Peter's finding had already been closed by his own session before the cycle ran. **Spot-verified on Drive 2026-09-18**, not taken on trust from the ledger: `Eugene/change-log/change-log-2026-09-16-claude-md-edit-backfill.md` (1,426 B) and `Fishbone Waste/Outputs/change-log-2026-09-16-house-rules-restructure-backfill.md` (1,313 B) — the latter documenting the exact 2026-09-11 Group house-rules restructure this issue named. Raising a group issue asking people to do what has already been done would be noise, so none was raised. Nothing was written to the group `open-issues.md`. |
| AX-5 | 2026-09-15 | Resolved | **Concurrent-session control-file collision during the Rung-2 release.** While this session authored Alex's Rung-2 charter (off a slightly older copy of `CHARTER.md`), a parallel session added **§9a (Hub-wide write authority)** to the charter. The two edits briefly produced two `CHARTER.md` variants — one carrying §9a, one carrying the Rung-2 release (pushed to git as `99652ed`, which had dropped §9a). **Caught before finishing** by re-reading the live control files (Alex's own `current-state.md` revealed §9a had already landed); the two variants were merged into a single **v3** charter (both Rung 2 and §9a), both predecessors archived-then-recreated to one live `CHARTER.md` (id `1CyMXpJ_id-EDu2eli2jOPfLmAoSHUNTf`, 19961 B), and git corrected to v3 (`18ab599`). No data lost. **Resolved 2026-09-15 with a standing lesson (now Alex's own discipline, and worth propagating estate-wide):** before any archive-then-recreate write to a control file, re-read that file's *current* live id and size immediately beforehand and author the edit onto that live copy — never onto a copy read earlier in the session — so a concurrent write cannot be silently overwritten or duplicated. Related to AX-4's undocumented-edit pattern (both are concurrency/attribution drift); logged for the team as a housekeeping convention, not a tooling blocker. |
| AX-6 | 2026-09-21 | Open | **Group KB: canonical document-numbering-and-filing policy has no live copy.** Found during this weekly Housekeeping sweep: `Wiki/Process-Document-Numbering-and-Filing.md` v1.3 was archived 2026-09-20 with a title stating "superseded by v1.4 — FG-CR-0001 finance-document rulings" (id `1-2_KJz4u3TvwENqnQSoz9JcJ_09g1UwC`), but no v1.4 file exists anywhere in the Wiki folder — confirmed by an exact-title search and a broader title-contains search, neither returning a live successor. `FG-CR-0001` (Document System - Change Requests sheet, row `3265778940512132`) is Status **Accepted**, Reviewed 2026-09-20, with a `Resolution / new version` field summarising the intended v1.4 content — so the decision landed but the live article was never (re)written. Same archive-fired/recreate-skipped concurrency failure pattern as AX-5. **Not Alex's to fix:** Alex was only given the Resolution field's summary, not the full v1.4 text, and authoring the policy document itself is substance (Rung 4). Logged to the group `open-issues.md` as **OI-17** (Rung-1 append, safe — see AX-3's resolution). Needs the session that authored FG-CR-0001's resolution, or Minda/Eugene, to recreate the live article. |
| AX-7 | 2026-09-21 | Resolved | **Own-KB housekeeping found and fixed this sweep.** (1) Three archived `processed-items-ledger.md` predecessors (items 49-59, 71-72, 71+73; all dated 2026-09-19) were sitting directly in this KB's root instead of `Archive/` — a duplicate-control-file-in-root drift the charter explicitly flags. Fixed: moved into `Archive/` (Drive `update_file` parentId change, metadata-only, file ids and content unchanged). (2) `change-log/` had no entry for the 2026-09-21 Rule C rollout session that `current-state.md`'s own "Last session" field already documented as complete — backfilled from that account (`change-log-2026-09-21-rule-c-rollout-backfill.md`; first upload attempt auto-converted to an empty Google Doc via `create_file`'s default conversion behaviour, caught by byte-verification, archived as an upload-error artifact, and redone correctly with `disableConversionToGoogleType: true` — a tooling gotcha worth remembering, distinct from AX-3). **Still open, not backfilled this session:** `change-log/` also has no entry for the 2026-09-20 sessions (the Rules A+B rollout itself, the reporting-reliability escalation) — this sweep did not have enough first-hand detail to reconstruct those safely without guessing; flagged in the `Sweeps/2026-09-21_Housekeeping-Sweep_v1.md` digest for the next attended session to complete. Resolved as far as this sweep could take it. |
| AX-8 | 2026-09-21 | Resolved | **Hub Drift Watch false-positive drift report, root-caused and fixed (`HL-0033`).** Ledger items 115-118 reported 16 real Tasks & Requests / Help & Lessons rows as not existing in either sheet, after a claimed fresh Smartsheet read. A direct re-check (this session) found all 16 fully intact, with real content, statuses and dates - nothing was ever deleted or fabricated. The flagged run's own read had come back with row counts and highest-IDs matching the sheets' state from roughly a day earlier (36 rows/`AWT-0037` on Tasks & Requests, 24 rows/`HL-0023` on Help & Lessons), despite the routine's own instructions asking for a fresh read each time - confirmed no duplicate sheet exists and the routine's stored prompt names the correct live sheet ids, so the balance of evidence points to a stale/cached read that was reported as current fact rather than flagged as a regression. Logged as `HL-0033` (Resolved) on the Hub. **Fixed**: drafted and (Minda, via the routines form - Alex cannot `update_trigger` on a routine it did not create) applied a **stale-read guard (step 2a)** to the Hub Drift Watch routine's own prompt - it now compares each read's row count/highest-ID against the board mirror's own count for that collection before trusting it (the board can only be at or behind Smartsheet, never ahead), re-reads once if it looks regressed, and reports the regression itself rather than naming rows as missing. Verified byte-identical against the live trigger. **Already confirmed working in production**: ledger item 126 shows the guard catching a genuine tool-side sampled/truncated read (`isSampled=true`, 47 of 55 rows) and correctly re-reading with narrower columns, rather than repeating the same false-positive. |
| AX-9 | 2026-09-21 | Resolved | **Scheduled routines not centrally reading Rules A-C from the charter.** Following `AX-8`, audited all 11 of the estate's scheduled Routines for whether each re-reads its own charter/current-state fresh every run - the mechanism that lets a rule change (Rule C, or any future rule) reach a routine automatically rather than needing a hand-copied patch each time. Helen's and Eugene's Task Check-in routines, Peter's Companies House research routine, and the group master-index sync already did this correctly. Found four that did not: **Victoria's Morning and Afternoon Coordination Sweeps** and **Peter's group-inbox-triage routines** had no such step at all; **Alex's own weekly Housekeeping sweep** ambiguously cited both the live Drive `CHARTER.md` and the `minda-ui/Alex` git mirror as if interchangeable, when the git mirror is confirmed stale (still v3, missing the Hub Coordination Standard and Rules A-C entirely - see the Git mirror row above). Two unrelated routines (Household Servicing weekly check-in, Minda's 6-month check-in) are on different systems entirely and were correctly left alone. **Fixed**: drafted and (Minda, via the routines form) applied a "read your charter fresh, apply Rules A-C" step to all four routines, and disambiguated Alex's own sweep to read live Drive, never git. All four verified byte-identical against their live triggers. |
| AX-10 | 2026-09-21 | Resolved | **Peter's inbox-triage frequency raised from 2x/day to 6x/day** (07:00/09:00/11:00/13:00/15:00/17:00 UK, every day of the week, per Minda's explicit decision to keep weekend coverage rather than restrict to weekdays). The underlying triage logic already dedups by Gmail thread-id against Peter's own processed log, so it generalises to any run count without a logic change - only the prompt's "twice a day" framing needed generalising. Planned (UK-to-UTC cron conversion for current BST, flagging the same twice-yearly DST-shift maintenance that already applied to the original 2 runs, now spanning 6); Minda repurposed the 2 existing triggers and created 4 new ones, all sharing the updated prompt (which also carries the `AX-9` charter-fresh-read step). All 6 verified directly against the live triggers - cron expressions and prompt content byte-identical - plus a follow-up trigger-naming cleanup, also verified. **Open reminder, not a defect:** the UK-clocks-change DST bump (last Sunday of October) now needs applying across 7 scheduled triggers (6 Peter + Hub Drift Watch is hour-agnostic so unaffected) instead of 2; tracked in `current-state.md`'s Next action list. |
| AX-11 | 2026-09-21 | Open | **Paper-mail item processed end-to-end; notify-Irina step still in flight.** A physical Council Tax reminder notice (North Tyneside Council, Account 45932043, 131 Goathland Avenue) was scanned by Minda into the group KB's `Raw/Paper Mail/`. Read via a local-file Read-tool workaround, since this environment had no OCR toolkit at all (`pdftoppm`/`pdftotext`/`tesseract`/`pdfimages`/Python `fitz`/`pytesseract` all absent) — the Read tool's native multimodal PDF support rendered the scanned content directly. Cross-referenced the Document Register (`FP0000101`/`FP0000102`/`FP0000103`) to confirm 131 Goathland Avenue is property **FP2401**, a Fishbone Properties Ltd asset. Registered the notice as **`FP0000145`** in the Group Document Register, filed it into `FP2401 - 131 Goathland Avenue/Correspondence/`, and routed a §7a hand-off note into Fishbone Properties Ltd's own `Raw/`. **Correction along the way:** Alex initially recorded the named addressee, Mr A Prutkovas, as "guarantor/director" on the property (extrapolating from the surname matching an existing Landbay guarantor, Andrejus Prutkovas). Minda corrected this — he is a family member (father), not a company officer or guarantor himself; the Document Register row and the hand-off note were both corrected to reflect that the council-tax liability sits with FP2401 regardless of who the notice names, a lesson in not over-inferring identity from a surname match. Raised Hub row **`AWT-0058`** (Critical) asking for Irina to be notified, since Alex has no Gmail connector and never sends mail itself. **A live concurrency race followed, caught and corrected rather than left inconsistent:** while Alex was reassigning `AWT-0058` from Peter to John (on a mistaken belief this was John's domain, since he owns the Properties KB), Peter's own six-times-daily routine independently picked up the row, drafted the notification email to Irina (cc Minda) via Gmail, and marked it Done — all within the same edit window. Alex's write landed after Peter's, requiring the "Assigned to" field to be corrected back to Peter to match who actually did the work. Minda then clarified the domain question directly: Peter's `ops@fishboneconstruction.co.uk` is the shared hub mailbox all six group companies' business email, in and out, routes through; John's `ops@fishboneproperties.co.uk` is a separate, KB-specific mailbox for Properties' own email intake pipeline and had no bearing here, since this item arrived as a paper scan, never an email. Alex's "domain boundary" note was withdrawn and the row's own request text corrected to record why there was no boundary issue. Minda then caught a second gap: the draft (id `r2667558814895859494`, thread `1a0c44bef247c34e`) summarised the notice in text but did not attach the scan itself. `AWT-0058` was reopened (**Status: In Progress**) with an explicit instruction for Peter to attach the scan (Drive id `1MtYbD-wz6YvAEEPPZMhUa4vCMpmaexZU`) to the existing draft on his next run. **Still open:** confirm at the next session that the attachment was added and that Minda sent the email to Irina before the 23-Sep-2026 deadline (£5.95 outstanding, rising to £773.00 with legal proceedings threatened if unpaid). **UPDATE 2026-09-21, later same session:** Peter's next scheduled run attempted exactly that - but correctly declined to force it. The scan's base64 encoding was 661,801 characters - far too large to safely retype as a single tool-call parameter without risking an undetected single-character corruption, with no attachment-side integrity check available (unlike Drive's byte-size check on upload) - a second, larger recurrence of the same `HL-0014` binary-relay gap previously seen only on Drive uploads up to 159KB. Instead updated the draft body/HTML to add a direct link to the source scan so Irina/Minda can open the original from the email even without a literal attachment. Left Status In Progress rather than Done, since the literal ask wasn't fulfilled - Minda to judge whether the link is sufficient or a human should attach the file manually. Flagged directly to Minda given the deadline is now imminent - remains Open. |
| AX-12 | 2026-09-21 | Resolved | **Roster duplicate row for John, found and removed.** Minda asked directly whether Irina/John/Darius were on the Roster, prompting a fresh read that turned up two rows for John: one still Status=Building (routines "none migrated yet"), one Status=Active (routines "being handed to John") - the classic "one duplicate = a bug" shape. Re-confirmed both rows live immediately before acting (Rule C); the Building row (id `5220517341169540`) was strictly subset by the Active one, nothing unique. Removed via Smartsheet `delete_rows` (permanent, no API-level undo - unlike a Drive file, a Smartsheet row has no archive-never-trash equivalent, so the full removed content was quoted back to Minda in chat for the record instead). Roster is now 10 rows, one per person. |
| AX-13 | 2026-09-21 | Resolved | **Achievements' `Employee` picklist was missing 6 of 10 Roster names - root cause of a false "missing achievements" report.** Minda reported she couldn't find Irina's or John's achievements from today. Direct queries against the Achievements sheet (`4569101748012932`), bypassing any UI filter, confirmed all four of today's rows for Irina/John were fully intact and correctly dated/attributed - nothing was lost or misrecorded. Root cause: the sheet's `Employee` column (id `8439906587772804`) is a PICKLIST configured with only 4 options (Peter/Eugene/Helen/Alex) against a 10-person Roster, and its `validation` flag was `false`, so Smartsheet's API silently accepted "Irina" and "John" into cells anyway - a value outside a picklist's configured options can be silently dropped by a UI view that filters or groups by that column's dropdown, exactly matching what Minda saw. Fixed at Minda's request: widened the column's options to all 10 current Roster names plus Minda herself (per her own "don't forget me") - no existing cell values touched. Lesson worth keeping: a PICKLIST column with `validation: false` can silently hold values its own dropdown doesn't list - worth checking other Hub picklist columns (Tasks & Requests' `Assigned to`, Help & Lessons' `Raised by`) against the current Roster next sweep. |
| AX-14 | 2026-09-21 | Open / Watch | **Irina's tenant compliance-packet sends - delivery unconfirmed for 7 of 10.** An Achievement row logged Irina emailing current compliance certificate packets (the genuine Gas Safety/EPC/EICR/Deposit/Renters' Rights set) to tenants across 10 properties in one afternoon. At Minda's direct request, checked whether they actually reached tenants - only 3 of 10 (FP2301/Luiza Konkina, FP1902/Yurii Kindrat, FP1602/Stephen Easton) have an explicit tenant-reply confirmation on record; the other 7 (FP2103, FP1901, FP2102, FP2003, FP2101, FP2004, FP2002) have no confirmation or bounce-check either way. Alex has no Gmail connector so could not check directly; offered to raise a Hub task for John (read access to the same mailbox) to check for bounce-backs without contacting any tenant directly. **Minda declined action for now** ("No, all good for now"). Left Open/Watch rather than closed, since the underlying gap (no delivery confirmation for 7 compliance emails) is still real, simply not urgent enough to act on unprompted; revisit only if Minda asks again or a tenant later reports non-receipt. |
| AX-15 | 2026-09-25 | Open | **Fishbone Properties Ltd's cloud routines run under a separate `ops@fishboneproperties.co.uk` login, invisible to the group master index.** Per Minda's owner note (2026-09-24, `Raw/2026-09-24_owner-note_properties-routines-on-ops-login.md`): the Properties KB's "8 cloud routines" (John's roster entry; group `CLAUDE.md` §1 Properties row) run under `ops@fishboneproperties.co.uk`, a different Google account from the group's `minda@fishboneconstruction.co.uk`. A group-login session's Routine list does not surface them at all — confirmed 2026-09-24: the account-wide list showed 16 routines, none of them John's or Properties'. Reviewing "how John is handling his routines" needs a session signed in as that `ops@` login specifically, not the group login. Raised by Victoria as **OI-18** (her own numbering, on whichever register she logs to — cross-referenced here as an `AX` row, not duplicated there). Related, in flight: John's new daily-intake routine (`AWT-0099`) is being created on that same `ops@` login, replacing the old `fishbone-daily-inbox-raw` routine (paused, not deleted, 14-day rollback); his dry-run-then-tick gate has separately been lifted for Smartsheet + Drive only (`AWT-0100`, Minda's ruling 2026-09-24 — this is the "still attended, never cut over" item flagged in the `Who to Ask` dashboard the day before, now resolved). **Not Alex's to fix** — an account/tooling-visibility gap, not a KB content issue, and Alex has no `ops@fishboneproperties.co.uk` connector to review it from. Per the owner note, once John's new routine is live the plan is to record the live Properties routine set + state from that login — needs someone actually signed into it. **CORRECTION 2026-09-25 (Alex, attended, verified directly — Rule C):** the visibility claim does not hold from here. This group-login session listed **all 19** Properties/John routines with their run history (`list_triggers`) and read their stored prompts (`get_trigger`) — e.g. `trig_0141naNrJBoadzztoBPVxyk8` (John's 06:15 intake) and `trig_01Fg3X9Vpx4jmTUnEqjDHkdo` (the paused 06:10 one). The same session could **not** see Alex's own Hub Drift Watch (`trig_01EMsc8Bn7c3a75q9cfr981c` returns not-found), so visibility appears to follow which account created a routine, not which login runs it — the "invisible to the group master index" framing is inaccurate, not the underlying fact that the routines run under `ops@`. Reviewing John's routines is therefore possible from a group-login session; editing a routine's prompt still needs its owner in the routines form (done this way 2026-09-25 for the 06:15 intake — see `Sweeps/2026-09-25_Properties-KB-Check_v1.md`). Stays Open only as the record of which login runs them. |

_(**All issues resolved as at 2026-09-18** were true at that time. AX-1, AX-2 resolved 2026-09-14; AX-5 resolved 2026-09-15; AX-3, AX-4 resolved 2026-09-18. AX-6 opened 2026-09-21 (group KB, needs a human/Eugene to recreate the missing policy article) - remains Open. AX-7 opened and resolved 2026-09-21 (own-KB root duplicates archived, one change-log gap backfilled; a second, smaller change-log gap from 2026-09-20 remains and is flagged in the Sweep digest, not tracked as its own AX issue). AX-8, AX-9, AX-10 opened and resolved 2026-09-21, same day, in a later attended session following the Rule C rollout and the weekly sweep: a Hub Drift Watch false alarm root-caused and fixed with a stale-read guard (already proven working in production), a routine-centralisation audit that found and patched four scheduled routines missing a charter-fresh-read step, and Peter's inbox-triage frequency raised from 2x/day to 6x/day. AX-11 opened 2026-09-21, later the same day still: a paper-mail item processed end-to-end into the Document Register and Properties KB, with a correction to an over-inferred identity claim, a live concurrency race with Peter's own routine caught and corrected rather than left inconsistent, a withdrawn mistaken domain-boundary note, and a Peter tooling-limit update later the same session - remains Open, deadline imminent. AX-12 and AX-13 opened and resolved 2026-09-21, same session again, following a direct Hub/dashboard health check Minda asked for: a stale Roster duplicate for John removed, and a misconfigured Achievements picklist (root cause of a reported "missing achievements" gap) found and fixed. AX-14 opened 2026-09-21, same session: a real but low-urgency delivery-confirmation gap on Irina's tenant emails, left Open/Watch at Minda's own call. AX-15 opened 2026-09-25, per `AWT-0098`: Properties' cloud routines run under a separate `ops@fishboneproperties.co.uk` login invisible to the group master index, surfaced by Victoria's routine review and raised by her as OI-18 — needs a session on that login, not Alex's to fix. AX-15 corrected the same day: its visibility claim does not hold from a group-login session, which can list and read the Properties routines. Resolved issues keep their full history above; nothing is deleted.)_
